{
  "name": "OwLLM Desktop",
  "goal": "Local-LLM agentic-team desktop app (Tauri + React). Source stays PRIVATE (ruigro/LLM-Studio); releases publish to OwLLM/owllm.",
  "verify": {
    "command": "cd owllm-desktop && npm run build",
    "lanes": {
      "frontend": "cd owllm-desktop && npm run build",
      "backend": "cd owllm-desktop/src-tauri && cargo check",
      "full": "cd owllm-desktop && npm run build"
    }
  },
  "release": {
    "versionFile": "owllm-desktop/src-tauri/tauri.conf.json",
    "versionScheme": "odometer-100",
    "stagePath": "owllm-desktop",
    "repo": "OwLLM/owllm",
    "mode": "host",
    "sign": {
      "thumbprint": "86D13D491414359FD09379181D4D1CC2B5E4E6AF",
      "tsa": "http://time.certum.pl"
    },
    "command": "bash \"owllm-desktop/scripts/publish-release.sh\" --notes \"$OWLLM_RELEASE_NOTES\""
  },
  "_note": "THE PROJECT CARD — per-project rules / goal / release / verify, committed so it travels with the repo. release{} is read by the rule-based publish (finish-and-publish.sh); defaults preserve OwLLM behaviour so this is non-breaking. Next: fold in verify + rules + mode + a UI to edit it, then a Steward agent that reviews this card for incongruences (user-approved fixes)."
}
