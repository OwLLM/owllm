# Auto-generated by core/profile_sync.py
# Source profile: ada_cu124
# Do not edit manually. Edit profiles JSON instead and re-run the generator.

typing-extensions==4.12.2
numpy==1.26.4
transformers>=4.57.0,<5.0
tokenizers>=0.21.0,<0.24.0
safetensors>=0.4.5,<0.6
huggingface-hub>=0.34.0,<1.0
peft>=0.13.0,<0.16.0
accelerate>=1.2.0,<1.3.0
bitsandbytes>=0.45.0,<0.50.0
datasets>=3.2.0,<4.0.0
PySide6==6.8.1
PySide6-Essentials==6.8.1
PySide6-Addons==6.8.1
sympy==1.13.1
jinja2==3.1.6
# fsspec capped at 2025.3.0 — datasets 3.x requires fsspec<=2025.3.0
# (3.2 caps even tighter at 2024.9.0). Older 2025.9.0 pin came from a
# different profile that wasn't checked against the datasets ceiling.
fsspec>=2024.9.0,<=2025.3.0
filelock==3.20.1
mpmath==1.3.0
regex==2024.11.6
pyyaml==6.0.2
requests==2.32.3
packaging==24.2
tqdm==4.67.1
sentencepiece==0.2.0
evaluate==0.4.3
einops==0.8.1
timm==1.0.12
open-clip-torch==2.29.0
pandas==2.2.3
psutil==7.2.0
streamlit==1.41.1
uvicorn>=0.30.0,<1.0.0
fastapi>=0.115.0,<1.0.0
pydantic>=2.0.0,<3.0.0
# OWLLM Agents tab — backend SDKs.
# anthropic powers the Claude API route; openai powers the Codex/GPT API route.
# These ship as MANDATORY dependencies because the Agents tab cannot let
# the user *select* an API backend if the SDK isn't already importable.
anthropic>=0.40.0,<1.0.0
openai>=1.50.0,<2.0.0
# OWLLM multimodal — voice-message transcription for in-app chat and the
# Telegram / WhatsApp bridges. CTranslate2-backed Whisper, no compile,
# downloads the chosen model file (~145 MB for "base") into
# LLM/runtime/whisper/ on first use. If absent, audio attachments still
# flow through the pipeline but arrive without a transcript.
faster-whisper>=1.0.0,<2.0.0
