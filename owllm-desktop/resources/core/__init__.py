# Minimal core package bundled with the ML scripts (finetune.py imports
# core.model_compatibility). Only the modules the bundled scripts need live here;
# the full core/ tree stays in python-app/ and is NOT shipped.
